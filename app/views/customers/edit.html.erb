<%= simple_form_for @customer do |f| %>
  <%= f.input :name, label: 'Name' %>
  <%= f.input :address, label: 'Address'%>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>

<%= link_to 'edit password', edit_customer_registration_path %>
