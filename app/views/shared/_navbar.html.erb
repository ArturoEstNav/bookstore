<div class="navbar navbar-expand-sm navbar-light navbar-custom fixed-top" >
  <%= link_to root_path, class: "navbar-brand" do %>
    The bookstore
  <% end %>
  <button class="navbar-toggler" type="button">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto d-flex align-items-center">
    <% if merchant_signed_in? %>
      <%= link_to 'Log out', destroy_merchant_session_path, method: :delete %>
    <% elsif customer_signed_in? %>
      <%= link_to 'Log out', destroy_customer_session_path, method: :delete %></h2>
    <% else %>
      <%= link_to 'Log in', new_merchant_session_path  %>
    <% end %>
    </ul>
  </div>
</div>

